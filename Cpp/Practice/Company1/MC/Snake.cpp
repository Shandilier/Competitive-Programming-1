#include<bits/stdc++.h>
using namespace std;

class Snake
{
    private:
        int head,tail;
    public:
        Snake(int head,int tail)
        {
            this->head = head;
            this->tail = tail;
        }
};